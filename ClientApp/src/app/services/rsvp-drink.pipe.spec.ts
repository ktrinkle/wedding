import { RsvpDrinkPipe } from './rsvp-drink.pipe';

describe('RsvpDrinkPipe', () => {
  it('create an instance', () => {
    const pipe = new RsvpDrinkPipe();
    expect(pipe).toBeTruthy();
  });
});
